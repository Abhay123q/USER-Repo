{
  "name": "user-management-app",
  "version": "1.0.0",
  "description": "User Management Dashboard with CRUD, Notifications, and Analytics",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate": "mysql -u root -p < sql/schema.sql",
    "seed": "mysql -u root -p < sql/schema.sql"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mysql2": "^3.6.0",
    "nodemailer": "^6.9.4",
    "pg": "^8.23.0",
    "serverless-http": "^4.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
